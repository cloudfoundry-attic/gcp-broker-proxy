---
applications:
  - name: gcp-broker-proxy
    memory: 64M
    instances: 1
    buildpack: binary_buildpack
    env:
      USERNAME:
      PASSWORD:
      BROKER_URL:
      SERVICE_ACCOUNT_JSON: |
      {
      }
